---
us: US13
title: Honor-rate de hints (lado emisión) + hint feature→/flow + skill-advisor fuera de /flow
wave: WB
depends_on: []
tdd_mode: forced
estimate: M
status: closed
closed: 2026-08-05
---

# US13 — Medir antes de endurecer (registro de cierre)

Ejecutada desde la especificación del index. Doctrina de la HU: "medir con
datos ANTES de endurecer" — el entregable es la MEDICIÓN, no más mandatos.

## Entregado (TDD forced — red→green honrado, 7 tests nuevos)

| Pieza | Detalle |
|---|---|
| Log de emisión de hints | `skill-activation.ts` escribe 1 línea JSON por hint emitido en `<cwd>/.claude/learned/skill-hints.log` ({ts, skills, flow}); fail-silent por contrato de hook. El lado de carga ya existía (`instructions-loaded.log`, 017/US12) → honor-rate = cargas que siguen a emisiones, calculable ad-hoc cuando haya datos (Cmd VII reactivo) |
| Hint feature→/flow | `detectFeatureShape()` con patrones multi-palabra conservadores ("nueva funcionalidad", "feature completa", "de principio a fin"…) → línea "consider /flow — full lifecycle" dentro del mismo bloque de hint. El hook solo descubre SKILLS de disco y /flow es un command — nunca lo proponía; era el hueco por el que el pipeline no se ofrecía en trabajo ad-hoc |
| `analyzePayload()` | Refactor: devuelve {injection, skills, flowHint} para el log; `processPayload()` queda como wrapper string-compatible (cero roturas — los tests previos siguen verdes) |
| skill-advisor fuera de /flow | YA entregado por US-dev (etapa PLAN del dev loop) + refactor CLAUDE.md (§Skill routing) — verificado, no duplicado |
| Docs-sync | Fila de UserPromptSubmit en `rules/paths/hooks.md` actualizada en la misma HU |

## Verificación

- Suites: hooks 150/150 (7 nuevos) ✅
- Smoke en vivo: prompt feature-shaped → bloque con Skill(scope) + skill-advisor + línea /flow, y `skill-hints.log` escrito con `{"skills":["scope"],"flow":true}` ✅
- Deferred por naturaleza: el honor-rate REAL necesita sesiones acumulando datos — la pregunta a los datos se hace cuando el log tenga volumen (1-2 semanas de uso).
