---
us: US18
title: Portabilidad multi-modelo/multi-harness (AGENTS.md + reparación Windows)
wave: WC
depends_on: []
tdd_mode: forced
estimate: M
status: closed
closed: 2026-08-05
---

# US18 — Portabilidad (registro de cierre)

## Entregado

| Pieza | Cuándo |
|---|---|
| CLAUDE.md agnóstico de modelo/harness ("the AI agent", modelos como ejemplo-entre-paréntesis) | v1, misma tarde de la directiva |
| `AGENTS.md` → symlink a CLAUDE.md en la raíz (Codex/Cursor/Gemini leen la misma doctrina) | v1 |
| `materializeAgentsMd()` en sync-claude + wiring en el execute path — repara el symlink-roto-como-texto de los checkouts Windows (git sin symlink support) copiando la doctrina real | Red→green, 4 tests (9/9 commands suite) |

## Deferred con razón (YAGNI — rung 1 de la escalera)

Homes globales por herramienta (`~/.codex/AGENTS.md`, config de Cursor/Gemini):
requiere research por tool y solo paga cuando esas herramientas se usen de
forma regular. Se activa cuando ocurra — no antes.

## Verificación

- Suite commands 9/9 (4 nuevos, fixture simula el estado Windows) ✅
- Symlink sano en este Mac → `materializeAgentsMd` = "ok" (no-op) ✅
- La reparación real en Windows se confirma en el próximo sync allí (deferred
  por plataforma, mismo cubo que el re-sync pendiente).
