---
us: US11
title: T8 workflow pack
wave: W3
depends_on: [US3]
tdd_mode: skip
estimate: M
status: closed
approved: 2026-09-10
closed: 2026-09-10
---

# US11 — T8 workflow

## Execution prompt (Phase 3 input)

**Task**: Write the T8 workflow pack. Codex cell is **ausente** as a file type (plugins = T7). Grok `.grok/workflows/*.rhai` + `/create-workflow`. Claude dynamic workflows. Native creator AC26 for Grok `create-workflow`.
**Context**: Grok `05-configuration.md` `[workflows]`; bundled `create-workflow` skill. Codex has no `.rhai`. Do not invent a Codex workflow format.
**Constraints**: D13 — never clone Claude/Grok into Codex. D7 — workflow is a type, not a meta-harness entrypoint.
**Deliverable**: `references/t08-workflow.md` + pointer to native-creators (from US3).
**Verify**: Codex column = «ausente»; Grok column names `.rhai` and disable `[workflows] enabled=false`.
**Ask first**: nothing — Codex absence already in spec catalog.

## ⚡ Quick reference

| Campo | Valor |
|---|---|
| **Status** | 🟡 draft |
| **Wave** | W3 |
| **Depends on** | US3 |
| **Blocks** | none |
| **Files touched** | `t08-workflow.md` |
| **TDD-mode** | skip: documentation pack |
| **Estimate** | M |
| **Cómo arrancar** | Read create-workflow SKILL + Grok user-guide workflows; write ausente for Codex |
| **Decisión absorbida** | D13 AC26 |

## User story

- **As a**: Lead authoring a Grok workflow
- **I want**: the rhai contract plus “Codex does not have this type”
- **So that**: I do not fake a Codex workflow with a plugin

## Acceptance criteria

- **AC1**: Given T8 pack, when Codex is requested, then the cell is ausente (not a plugin tutorial).
- **AC2**: Given Grok `create-workflow`, when the artifact enters this repo, then meta-harness still owns gate/impact (AC26).

tdd-skip: documentation pack, no testable behavior
