---
parent: harness-config
name: t09-memory
description: T9 project memory pack — CLAUDE.md / AGENTS.md. Grok dual-load. Cross-session Grok memory is not this type.
---

# T9 — Project memory

Shared: [lookup.md](lookup.md) · [lifecycle.md](lifecycle.md) · [t04-rule.md](t04-rule.md) · [t01-skill.md](t01-skill.md).
Absorbs cookbook `01-claude-md.md` (D27).

## 1. What / when

Always-do project identity (stack, commands, conventions). Hook (T5) when it must happen. Rule (T4) when path-scoped. Skill (T1) when it needs judgement.

Grok **cross-session** memory (`[memory] enabled`, `GROK_MEMORY=1`, `~/.grok/memory/`) is experimental and **not** this type.

## 2. Lookup

Claude memory / CLAUDE.md docs. Codex `learn.chatgpt.com` custom instructions + `/init`. Grok `12-project-rules.md` (not `13-memory.md`).

## 3. Vendor declare

| | Claude Code | Codex | Grok Build |
|---|---|---|---|
| Files | Managed; `./CLAUDE.md` or `./.claude/CLAUDE.md`; `~/.claude/CLAUDE.md`; `./CLAUDE.local.md`. Walks up from cwd | `AGENTS.md` layers. `/init` scaffolds. `project_doc_max_bytes`; `project_doc_fallback_filenames` when AGENTS.md missing | Loads matching names per dir: `Agents.md`, `Claude.md`, `CLAUDE.md`, `CLAUDE.local.md`, `AGENT.md`, `AGENTS.md`. Also `.grok/rules/*.md`. Trust required |
| Import | `@path` inline, max 5 hops | n/a as Claude `@` | Dual-load: a folder with both `AGENTS.md` and `CLAUDE.md` contributes both |
| Size | CLAUDE.md **<200 lines is guidance B**, not CI (D15) | Byte cap per file, not a line-count gate | Same: request cost, not `check-config` |
| Disable | `claudeMdExcludes` globs; omit the file | Don't ship / don't load that layer | Don't ship the file; `compat.claude.agents=false` stops Claude-tree named files (generic top-level `CLAUDE.md` still recognized) |
| Reload | Session; after `/compact` project-root re-injects | New session | Session start; trust |

HTML comments in Claude CLAUDE.md are stripped. Instruction = **request**, not a hook (H9).

## 4. Min template

Template N/A as a gate artefact. Short project file:

```markdown
# {{Project}}

## Stack
- {{language}} / {{runtime}}

## Commands
- {{test}} — {{what}}

## Conventions
- {{one always-do}}
```

Do not dump autogenerated trees here.

## 5. Evidence

E2: memory = request. E6: papers disagree on AGENTS.md payoff — no invented line-count CI. T1: Grok 12-project-rules.md 2026-09-10. **sin evidencia A/B** for an ideal CLAUDE.md length.

## 6. Poneglyph grain

This repo: `CLAUDE.md` + `AGENTS.md` addendum (Codex). Keep thin; details go to skills. Always-loaded growth hits the budget ratchet (AC30), not a new `check-config` line.

## 7. Absences

| Cell | Status |
|---|---|
| CLAUDE.md line-count in `check-config` | **ausente** (D15) |
| Grok cross-session `[memory]` | **ausente** from T9 — that is experimental session memory |
| Clone Claude `@import` into Codex | **ausente** |
