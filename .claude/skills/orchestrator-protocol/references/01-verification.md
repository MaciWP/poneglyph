---
parent: orchestrator-protocol
name: verification
description: Verification before asserting — canonical source is the `anti-hallucination` skill.
---

# Verification Before Asserting

> **Canonical source: the `anti-hallucination` skill.** Tool hierarchy (LSP > Grep > Glob > Read), confidence levels, domain-adaptive thresholds, the staged validation pipeline, critical-keyword forcing and the common hallucination patterns all live there. This reference previously duplicated them verbatim — collapsed to a pointer to kill the duplication (Commandment IX). Load `anti-hallucination` for the full patterns.
